## What I Will Add
- A simple numeric array: `const arr = [1, 2, 3]`
- One alternative (strings): `const fruits = ['apple', 'banana', 'cherry']`
- One nested example: `const nested = [1, [2, 3], [4, 5]]`

## Where
- Insert into `f:\Arman STUDY\8th semester\next level course\module-2\M02V01.js`

## Verification
- Log values with `console.log(arr)` (and the others) to confirm output in the terminal.

## Outcome
- You’ll have clear, ready-to-run examples of arrays in JavaScript.

## Request
- Confirm and I will insert the code and run it to show outputs.